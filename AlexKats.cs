using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlexKats : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("ok ok ok");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
